using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandomMutatant : MonoBehaviour
{
    // Start is called before the first frame update
    public int n_mut = 0;
    public bool fmi = false;
    public bool sdk = false;
    void Start()
    {
        //pick n random ids 
        // max row max col
        //or directly use inside params

        //use by State.cs to keep it inactive asll the time 
    }

    // Update is called once per frame
    void Update()
    {
        //wait till
    }
}
